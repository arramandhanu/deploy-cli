#==============================================================================
# STAGING ENVIRONMENT
# Copy this file to .env.staging for staging deployments
#==============================================================================

# Override remote server for staging
# REMOTE_HOST=staging.example.com
# REMOTE_USER=deploy
# REMOTE_COMPOSE_DIR=/opt/cpi-food-staging/compose

# Build Arguments for Next.js apps
NEXT_PUBLIC_APP_ENV=staging
NEXT_PUBLIC_API_BASE_URL=/api

# Database URL (for keystore service)
# DATABASE_URL=postgresql://user:pass@staging-db:5432/db
